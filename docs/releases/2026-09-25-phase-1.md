# Phase 1: brand and navigation finalization

Release date: 25 September 2026.

## Changes

- Use the supplied Marketing logo artwork in the header, footer and favicon.
- Open desktop navigation panels on click, with Escape and outside-click dismissal.
- Reset scroll and page-local tabs on route changes; keep FAQ category navigation within the FAQ page.
- Repair four obsolete article destinations and label service overview links accurately.
- Connect the footer Subscribe link to the newsletter page.
- Replace the developer-facing missing-page screen with branded navigation.

The existing page layouts, typography and colour palette are retained. Forms still require a production delivery service. The three proposed homepage section alternatives remain separate review items.

## Validation

TypeScript and production build pass. Browser checks cover both homepage destination links, menu hover/click/Escape behaviour, FAQ category scrolling, cross-service tab state, mobile navigation, obsolete URLs and the missing-page screen.

## Release source

Use branch `codex/site-finalization`, based on deployed commit `74a3a168d5dc339ab3f5be0a6f0e9426a1ca551d`. Do not replace it with the newer remote main branch without reconciling the differences recorded in the project inventory. The original Mac checkout contains unpublished work and remains separate.

The first release attempt was blocked because the Mac-wide Git author belonged to another account. The release commit uses Nic Tan's project identity, verified against the authenticated GitHub account, the previously successful production commit and the Vercel account. Mac-wide Git configuration is unchanged.
